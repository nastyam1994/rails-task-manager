<h1>My Tasks</h1>

<div>
  <% @tasks.each do |task| %>
    <input type="checkbox" id="t<%= @task.id %>" checked="<%= @task.completed %>">
    <label for="t<%= @task.id %>"><%= link_to @task.title, task_path(task) %></label>
  <% end %>
</div>

<%= link_to 'Add new task', new_task_path %>
